# This configuration is for Cloudflare Pages (frontend only)
# For the full setup, see DEPLOYMENT.md

name = "finance-frontend"
type = "javascript"

# Build settings for Cloudflare Pages
[build]
command = "npm run build"
destination = "dist"

[env.production]
name = "finance-frontend-prod"

[env.staging]
name = "finance-frontend-staging"
